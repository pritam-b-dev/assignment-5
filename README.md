- 1️⃣ What is the difference between var, let, and const?

  #Ans: A var allows to use same variavle name several times wheather let and const doesn't allows it. But let will allows to change value, on the other side const never allows to change values of defining variables.

- 2️⃣ What is the spread operator (...)?

  #Ans: Using spread operator we can make a copy, store or concate list of array or object to another variable.

- 3️⃣ What is the difference between map(), filter(), and forEach()?

  #Ans: map and filter returns an array where forEach doesn't return, only loop through over the array with callback function.

- 4️⃣ What is an arrow function?

  #Ans: On the update version of javascript ES6, an arrow function is the shortest way to define a function. but it doesn't support hoisting.

- 5️⃣ What are template literals?

  #Ans: Template literals reffers a modern process to write multy line string and also assigning severals variables into it by using backtick symbol.
